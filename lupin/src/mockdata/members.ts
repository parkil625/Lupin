import { Member, ChatMessage } from "@/types/dashboard.types";

/**
 * TEMPORARY STUB DATA
 * TODO: Replace with API calls
 *
 * 이 파일은 컴파일 오류를 방지하기 위한 임시 stub 데이터입니다.
 * 추후 실제 API 호출로 교체해야 합니다.
 */

export const members: Member[] = [];

export const initialDoctorChats: { [key: number]: ChatMessage[] } = {};

export const appointments: any[] = [];
